// Dependencies
import React, { PropTypes } from 'react';


const DeletePromotionModal = props => {

  return (
    <div>Delete</div>
  );
};

DeletePromotionModal.propTypes = {
  hideModal: PropTypes.func.isRequired,
};

export default DeletePromotionModal;